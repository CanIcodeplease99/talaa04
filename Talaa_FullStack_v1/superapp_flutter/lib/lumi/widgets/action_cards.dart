import 'package:flutter/material.dart'; class FxQuoteCard extends StatelessWidget{final double rate; final double fee; final double received; const FxQuoteCard({super.key,required this.rate,required this.fee,required this.received}); @override Widget build(BuildContext c){return Column(crossAxisAlignment:CrossAxisAlignment.start, children:[Text('FX Quote', style: Theme.of(c).textTheme.titleMedium), const SizedBox(height:8), Text('Rate: $rate'), Text('Fee: $fee'), Text('Recipient gets ≈ GH₵ $received'), const SizedBox(height:8), Row(children:[ElevatedButton(onPressed:(){}, child: const Text('Send with this rate')), const SizedBox(width:8), TextButton(onPressed:(){}, child: const Text('Refresh'))])]);}} class RailStatusCard extends StatelessWidget{final List<Map<String,dynamic>> items; const RailStatusCard({super.key, required this.items}); @override Widget build(BuildContext c){return Column(crossAxisAlignment:CrossAxisAlignment.start, children:[Text('Rails Health', style: Theme.of(c).textTheme.titleMedium), const SizedBox(height:8), ...items.map((e)=>Row(children:[Icon(e['status']=='healthy'?Icons.check_circle:Icons.error, size:16), const SizedBox(width:8), Text('${e['name']} • ${e['status']}')]))]);}}
